# Hillson Test OS - 开发总结文档

**日期**: 2025-12-30
**版本**: 1.0
**开发环境**: Windows 10 + WSL + GCC + QEMU/真机测试

---

## 📋 目录

1. [项目概述](#项目概述)
2. [主要成就](#主要成就)
3. [系统架构](#系统架构)
4. [用户Shell实现](#用户shell实现)
5. [内存管理系统](#内存管理系统)
6. [系统调用接口](#系统调用接口)
7. [调试历程](#调试历程)
8. [真机兼容性](#真机兼容性)
9. [构建和测试](#构建和测试)
10. [已知限制](#已知限制)
11. [后续开发计划](#后续开发计划)

---

## 🎯 项目概述

**Hillson Test OS** 是一个基于 x86 架构的简易操作系统内核，实现了基本的任务管理、内存管理和用户程序执行功能。

### 核心特性
- ✅ 用户程序加载和执行
- ✅ 交互式 Shell（支持多个命令）
- ✅ Buddy System 内存管理
- ✅ 完整的系统调用接口
- ✅ 键盘输入/屏幕输出
- ✅ 真机兼容（在 Dell 笔记本上测试通过）

---

## 🏆 主要成就

### 1. 用户 Shell 实现

**文件**: [simple_interactive_shell.c](test/simple_interactive_shell.c)

**支持的命令**:
| 命令 | 功能 | 示例 |
|------|------|------|
| `help` | 显示帮助信息 | `> help` |
| `mem` | 显示内存统计（人类可读格式） | `> mem` |
| `clear` | 清屏 | `> clear` |
| `echo <text>` | 回显文本 | `> echo hello` |
| `ver` | 显示版本号 | `> ver` |
| `exit` | 退出程序 | `> exit` |

**特色功能**:
- 完整的命令解析和参数处理
- 人类可读的内存显示（自动选择 KB/MB/GB 单位）
- 无栈溢出风险（使用全局变量）
- 代码简洁，易于扩展

### 2. 内存管理系统

**Buddy System 实现**
- 物理页分配器（4KB 页大小）
- 高效的内存分配/释放算法
- 支持大内存管理（测试过 4GB 内存）

**内存统计功能**:
```bash
> mem
Total: 3GB Free: 3GB Used: 128KB
```

### 3. 真机兼容性

**问题**: 早期版本在 QEMU 上正常，但在真机上报 GP Fault

**根本原因**:
1. `.bss` 段未正确映射
2. 段寄存器恢复不完整
3. 页表映射权限问题

**解决方案**:
- 修复 ELF 加载器，正确映射 `.bss` 段
- 在 `iret` 前手动恢复 DS/ES/FS/GS 段寄存器
- 统一用户/内核页表权限设置

---

## 🏗️ 系统架构

### 内存布局

```
物理内存布局:
0x00000000 - 0x003FFFFF    前4MB（内核代码/数据）
0x00400000 - 0x23FFFFFF    用户程序空间
0x2400000 - 0xBFFFFFFF    Buddy System 管理范围（36MB-3GB）

虚拟地址空间:
0x00000000 - 0x003FFFFF    低4MB（直接映射）
0x08000000 - 0x08000FFF    用户程序（4KB代码+数据）
0xB8000                   VGA 文本模式缓冲区
0xC0000000 - 0xFFFFFFFF    内核空间
```

### 任务管理

**Task 结构** ([task.h](include/task.h)):
```c
typedef struct task_t {
    struct trapframe *tf;       // 陷阱帧
    uint32_t task_total_count;
    uint32_t *pde;               // 页目录
    uint32_t *kstack;            // 内核栈
} task_t;
```

**Trapframe 布局** ([interrupt.h](include/interrupt.h)):
```c
struct trapframe {
    uint32_t edi, esi, ebp, oesp, ebx, edx, ecx, eax;
    uint32_t gs, fs, es, ds;
    uint32_t trapno, err;
    uint32_t eip, cs, eflags;
    uint32_t esp, ss;
};
```

---

## 💻 用户 Shell 实现

### 代码结构

**全局变量** (避免栈溢出):
```c
char cmdline[128];  // 命令缓冲区
int cmdlen;         // 命令长度
```

**主循环**:
```c
void _start() {
    while (1) {
        // 1. 显示提示符
        sys_putchar('>');
        sys_putchar(' ');

        // 2. 读取输入
        cmdlen = 0;
        while (cmdlen < 127) {
            c = sys_getchar();
            if (c == '\n') break;
            if (c >= 32 && c <= 126) {
                cmdline[cmdlen] = c;
                cmdlen++;
                sys_putchar(c);
            }
        }

        // 3. 解析命令
        if (cmdlen == 4 && ...) {
            // help 或 exit 命令
        }
        // ... 其他命令
    }
}
```

### 关键设计决策

1. **使用全局变量**: 用户栈只有 4KB，局部数组会导致栈溢出
2. **内联命令解析**: 避免函数调用开销
3. **简单的字符串操作**: 不使用标准库函数
4. **人类可读的内存显示**: 自动选择合适的单位

---

## 🧠 内存管理系统

### Buddy System

**核心数据结构** ([mm/buddy.c](mm/buddy.c)):
```c
struct buddy_zone {
    uint32_t base;       // 起始物理地址
    uint32_t size;       // 总大小（字节）
    uint32_t page_count; // 总页数
    int max_order;       // 最大分配阶数
    struct free_area free[MAX_ORDER];
};
```

**分配算法**:
- 按需分配：找到最小的合适块
- 伙伴合并：释放时自动合并相邻的空闲块
- 阶数管理：支持 2^0 到 2^10 页的分配

### 内存统计

**系统调用**: `sys_get_mem_stats`

**返回信息**:
- `total_pages`: 总页数
- `free_pages`: 空闲页数
- `used_pages`: 已用页数

**单位转换**:
```
1 页 = 4KB
< 1024 B    → 显示为 B
< 1 MB      → 显示为 KB
< 1 GB      → 显示为 MB
≥ 1 GB      → 显示为 GB
```

---

## 🔧 系统调用接口

### 系统调用列表

| 号码 | 名称 | 功能 | 参数 |
|------|------|------|------|
| 1 | SYS_PRINTF | 格式化输出 | `const char *fmt` |
| 2 | SYS_EXIT | 退出程序 | `int code` |
| 3 | SYS_YIELD | 让出CPU | - |
| 4 | SYS_GET_MEM_STATS | 获取内存统计 | `struct mem_stats *` |
| 5 | SYS_READ_MEM | 读取内存 | - |
| 6 | SYS_GETCHAR | 读取字符 | - |
| 7 | SYS_KBHIT | 检查按键 | - |
| 8 | SYS_PUTCHAR | 输出字符 | `char c` |

### 系统调用流程

```
用户程序
    ↓
syscall_entry.S (封装函数)
    ↓
int 0x80 (软中断)
    ↓
isr80_wrapper.s (中断处理)
    ↓
syscall_dispatch() [syscall.c]
    ↓
内核服务函数
    ↓
iret (返回用户模式)
```

---

## 🐛 调试历程

### 问题1: GP Fault (trapno=13)

**症状**:
```
=== GP Fault ===
EIP=0x8000044 CS=0x1B
DS=0x0 ES=0x0 FS=0x0 GS=0x0
```

**原因**: 段寄存器未正确恢复

**解决**:
```c
asm volatile (
    "popa\n\t"              // 恢复通用寄存器
    "popl %%eax\n\t"         // 弹出 gs
    "movw %%ax, %%gs\n\t"
    "popl %%eax\n\t"         // 弹出 fs
    "movw %%ax, %%fs\n\t"
    "popl %%eax\n\t"         // 弹出 es
    "movw %%ax, %%es\n\t"
    "popl %%eax\n\t"         // 弹出 ds
    "movw %%ax, %%ds\n\t"
    "addl $8, %%esp\n\t"     // 跳过 trapno/err
    "iret\n\t"               // 返回用户模式
);
```

### 问题2: .bss 段未映射

**症状**:
```
EIP=0x8000044 - 访问全局变量 cmdlen 时出错
```

**原因**: ELF 加载器只映射了文件中的数据，未映射 `.bss` 段

**解决** ([userboot.c](userboot.c)):
```c
for (uint32_t off = 0; off < memsz; off += PAGE_SIZE) {
    uint32_t dst_va = va + off;
    uint32_t dst_pa;

    if (off < filesz) {
        // 文件中的数据
        dst_pa = file_pa + off;
    } else {
        // .bss 部分：分配新的零页
        dst_pa = pmm_alloc_page();
        memset(phys_to_virt(dst_pa), 0, PAGE_SIZE);
    }

    map_page(pd_user, dst_va, dst_pa, USER_PTE_FLAGS);
}
```

### 问题3: 栈溢出

**症状**: 使用局部变量数组导致 GP Fault

**原因**: 用户栈只有 4KB

**解决**: 使用全局变量代替局部数组
```c
// 不好（栈溢出）
void func() {
    char buf[128];
}

// 好
char cmdline[128];
void func() {
    // 使用全局变量
}
```

---

## 🖥️ 真机兼容性

### 测试环境
- **机器**: Dell 笔记本
- **CPU**: x86 兼容处理器
- **内存**: 4GB+
- **测试日期**: 2025-12-30

### 兼容性改进

1. **严格的内存保护**
   - 页表权限检查
   - 用户/内核模式分离
   - CR3 正确切换

2. **精确的寄存器恢复**
   - 所有段寄存器正确设置
   - 栈对齐要求
   - EFLAGS 标志位处理

3. **按需页表映射**
   - 自动创建内核页表
   - TLB 刷新
   - 物理页清零

---

## 🔨 构建和测试

### 构建步骤

```bash
# 1. 编译内核
cd hillson_test_os
make clean && make

# 2. 编译用户程序
cd test
make clean && make

# 3. 创建 ISO 镜像
cd ..
bash zh.sh

# 4. 测试（QEMU）
qemu-system-i386 -cdrom os.iso -m 4096 -nographic

# 5. 真机测试
# 将 os.iso 刻录到 USB，从 USB 启动
```

### 测试清单

**基础功能**:
- [x] 内核启动
- [x] 用户程序加载
- [x] 键盘输入
- [x] 屏幕输出

**Shell 命令**:
- [x] `help` - 显示帮助
- [x] `mem` - 显示内存
- [x] `clear` - 清屏
- [x] `echo` - 回显文本
- [x] `ver` - 显示版本
- [x] `exit` - 退出

**内存管理**:
- [x] kmalloc/kfree
- [x] Buddy System
- [x] 内存统计

**真机测试**:
- [x] Dell 笔记本
- [x] 无 GP Fault
- [x] 所有命令正常工作

---

## ⚠️ 已知限制

### 1. 用户栈大小
- **当前**: 4KB
- **限制**: 不能使用大数组或深度递归
- **解决方案**: 使用全局变量

### 2. 单任务系统
- **当前**: 只支持一个用户任务
- **计划**: 实现多任务调度

### 3. 无文件系统
- **当前**: 所有程序在内存中
- **计划**: 添加磁盘 I/O 和文件系统

### 4. 简单的 Shell
- **当前**: 只支持简单的命令解析
- **计划**: 添加管道、重定向、后台任务

---

## 📅 后续开发计划

### 短期目标（1-2周）

1. **增强 Shell**
   - 支持命令历史（上下箭头）
   - Tab 键自动补全
   - 更多的内置命令

2. **多任务支持**
   - 实现任务调度器
   - 支持 fork/exec
   - 进程间通信

3. **文件系统**
   - 简单的 FAT32 支持
   - 文件读写操作
   - 目录操作

### 中期目标（1-2个月）

1. **网络支持**
   - 网卡驱动
   - TCP/IP 协议栈
   - Socket 接口

2. **更多设备驱动**
   - 鼠标支持
   - 声卡支持
   - USB 支持

### 长期目标（3-6个月）

1. **图形界面**
   - VGA 图形模式
   - 窗口管理器
   - 基本的 GUI 程序

2. **POSIX 兼容**
   - 标准 C 库（newlib/musl）
   - POSIX API
   - 常用 Unix 工具

---

## 📚 参考资料

### 内核开发资源

- **x86 架构**: Intel 64 and IA-32 Architectures Software Developer's Manual
- **OS 原理**: Modern Operating Systems (4th Edition)
- **xv6**: https://pdos.csail.mit.edu/6.828/2021/xv6.html

### 开发工具

- **GCC**: https://gcc.gnu.org/
- **QEMU**: https://www.qemu.org/
- **GRUB**: https://www.gnu.org/software/grub/
- **Git**: https://git-scm.com/

### 相关项目

- **Linux Kernel**: https://www.kernel.org/
- **Redox OS**: https://redox-os.org/
- **ToaruOS**: https://toaru-os.org/

---

## 📝 更新日志

### v1.0 (2025-12-30)
- ✅ 首次发布
- ✅ 用户 Shell 实现
- ✅ Buddy System 内存管理
- ✅ 真机兼容性改进
- ✅ 完整的系统调用接口

---

## 🙏 致谢

感谢以下项目和社区：
- xv6 (MIT)
- Linux 内核社区
- QEMU 开发者
- GCC 开发者
- 开源社区

---

**文档维护**: Hillson
**最后更新**: 2025-12-30
**项目地址**: https://github.com/hillson1979/hillson_test_os
