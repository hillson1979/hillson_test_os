/**
 * @file lv_types_compat.h
 * @brief LVGL 类型兼容层
 *
 * 提供 LVGL 需要的标准类型定义
 */

#ifndef LV_TYPES_COMPAT_H
#define LV_TYPES_COMPAT_H

#include "stdint_compat.h"

// bool 类型已经在 stdbool.h 中定义了（通过 -include 包含）
// 这里不再重复定义

#endif /* LV_TYPES_COMPAT_H */
